# Backend Configuration
NODE_ENV=development
PORT=3001

# Frontend Configuration
REACT_APP_API_URL=http://localhost:3001/api
REACT_APP_ENVIRONMENT=development

# Optional: External integrations (add credentials via GitHub Secrets, not here)
# GITHUB_TOKEN=<add via GitHub Secrets>
# DOCKER_USERNAME=<add via GitHub Secrets>
# DOCKER_PASSWORD=<add via GitHub Secrets>
