{
  "name": "pods-simulation",
  "version": "1.0.0",
  "description": "PODS Network Simulation Platform - Mature State 2035",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "test": "npm test --workspaces",
    "install-all": "npm install && npm install --workspace=backend && npm install --workspace=frontend",
    "clean": "rm -rf node_modules backend/node_modules backend/dist frontend/node_modules frontend/build",
    "docker:build": "docker build -f docker/Dockerfile -t pods-simulation:latest .",
    "docker:run": "docker run -p 3000:3000 pods-simulation:latest"
  },
  "keywords": [
    "PODS",
    "mobility",
    "simulation",
    "network",
    "react",
    "typescript"
  ],
  "author": "",
  "license": "MIT"
}
